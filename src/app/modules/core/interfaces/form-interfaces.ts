export interface address {
  country: string;
  city: string;
  street: string;
  state: string;
  zip: number;
}

export interface book {
  name: string;
  author: string;
  id: number;
}

export interface contact {
  phone: number;
  email: string;
}
